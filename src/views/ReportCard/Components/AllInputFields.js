// Export the new serverless AI component as AIInputField for backward compatibility
export { default as AIInputField } from '../../../components/AIReportCommentInput';

// Re-export it as AIReportCommentInput for new usage
export { default as AIReportCommentInput } from '../../../components/AIReportCommentInput'; 